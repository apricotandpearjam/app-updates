# app-updates

Sparkle update hosting for All Good Folks apps. The app's in-app updater reads
the appcast and downloads the DMG directly (customers never visit this repo).

- `trigger/appcast.xml` + `trigger/Trigger-*.dmg` — Trigger
